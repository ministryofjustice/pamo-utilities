[project]
name = "pamo-utilities"
version = "1.0"

authors = [
    { name = "MoJ", email = "" }
]
license = "MIT"
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    "pandas",
    "numpy"
]

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"
